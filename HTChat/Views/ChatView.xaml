<Page x:Class="HTChat.Views.ChatView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:HTChat.Views"
      xmlns:controls="clr-namespace:HTChat.Controls"
      mc:Ignorable="d" 
      d:DesignHeight="300" d:DesignWidth="300"
      Title="ChatView">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition Height="3*"/>
            <RowDefinition Height="1*"/>
        </Grid.RowDefinitions>
        <controls:ChatControl Grid.Row="1"/>
        <TextBox Grid.Row="3" AcceptsTab="True" AcceptsReturn="True" KeyUp="OnKeyUp" DragDrop.Drop="OnDrop"/>

    </Grid>
</Page>
