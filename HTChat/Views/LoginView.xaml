<Page x:Class="HTChat.Views.LoginView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:HTChat.Views"
      mc:Ignorable="d" 
      d:DesignHeight="300" d:DesignWidth="300"
      Title="LoginView">

    <Grid>
        <StackPanel Margin="10">
            <Label>Username</Label>
            <TextBox Text="{Binding Username}"/>
            <Label>Password</Label>
            <PasswordBox Name="pwBox"/>
            <Label>Domain</Label>
            <TextBox Text="{Binding Domain}" />
            <Button Margin="5 10" Height="30" Command="{Binding Login}" CommandParameter="{Binding ElementName=pwBox}">Login</Button>
            <CheckBox Margin="30 5" IsChecked="{Binding RememberLogin}">Remember me</CheckBox>
        </StackPanel>
    </Grid>
</Page>
